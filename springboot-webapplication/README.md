# springboot-application-elastiBeanStack

# Pre-Requisites
    Build Aritifacts
# Upload artifacts with in Elastic bean stack service
  select application type as "tomcat" and click on create application
# Check the output of application using link
